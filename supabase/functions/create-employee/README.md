# create-employee

Creates a Supabase Auth user and inserts a profile row in `public.users`.

## Deploy

supabase functions deploy create-employee

## Required secrets

supabase secrets set SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY
